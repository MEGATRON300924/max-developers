"use client";

import Link from "next/link";
import { useEffect, useState } from "react";
import { AppWindow, ExternalLink, KeyRound, RefreshCw, Settings2, ShieldCheck, Trash2, SlidersHorizontal } from "lucide-react";

type OAuthClient = { id: string; clientId: string; name: string; redirectUris: string[]; scopes: string[]; isActive: boolean };
type ApiResponse = { clients?: OAuthClient[]; message?: string };

export default function ApplicationsPage() {
  const [clients, setClients] = useState<OAuthClient[]>([]); const [loading, setLoading] = useState(true); const [error, setError] = useState<string | null>(null); const [revoking, setRevoking] = useState<string | null>(null);
  async function load() { setLoading(true); setError(null); try { const response = await fetch("/api/applications", { cache: "no-store" }); const data = await response.json() as ApiResponse; if (!response.ok) throw new Error(data.message || "Unable to load applications."); setClients(data.clients || []); } catch (err) { setError(err instanceof Error ? err.message : "Unable to load applications."); } finally { setLoading(false); } }
  useEffect(() => { void load(); }, []);
  async function revoke(client: OAuthClient) { if (!window.confirm(`Revoke “${client.name}”? Users will no longer be able to authorize this application.`)) return; setRevoking(client.id); try { const response = await fetch(`/api/applications/${encodeURIComponent(client.id)}`, { method: "DELETE" }); const data = await response.json() as { message?: string }; if (!response.ok) throw new Error(data.message || "Unable to revoke application."); await load(); } catch (err) { setError(err instanceof Error ? err.message : "Unable to revoke application."); } finally { setRevoking(null); } }
  return <div className="shell"><aside className="sidebar"><div className="brand"><div className="logo">M</div><div className="brand-copy"><div className="brand-name">MAX</div><div className="brand-sub">Developer Platform</div></div></div><nav className="nav"><div className="nav-group"><div className="nav-label">Platform</div><Link href="/" className="nav-item"><AppWindow/><span>Home</span></Link><Link href="/applications" className="nav-item active"><AppWindow/><span>Applications</span></Link><Link href="/credentials" className="nav-item"><KeyRound/><span>Credentials</span></Link><Link href="/activity" className="nav-item"><ShieldCheck/><span>Activity</span></Link></div></nav><div className="sidebar-foot"><strong>MAX AI Ecosystem</strong><br/>Build experiences that connect to MAX.</div></aside><div className="main"><header className="topbar"><div className="breadcrumb">Applications</div><div className="account"><div className="avatar">M</div></div></header><main className="content"><div className="hero"><div><div className="eyebrow">MAX Auth</div><h1>Applications</h1><p className="lead">All OAuth applications registered in MAX Auth are synced here.</p></div><div style={{display:"flex",gap:8,alignItems:"center"}}><button className="secondary" onClick={() => void load()} disabled={loading}><RefreshCw size={15} style={{verticalAlign:"-2px",marginRight:6}}/>{loading ? "Refreshing…" : "Refresh"}</button><a className="primary" href="https://auth.max-ai.name.ng/developer" target="_blank" rel="noreferrer"><ExternalLink size={15} style={{verticalAlign:"-2px",marginRight:6}}/>MAX Auth</a></div></div><section className="notice" style={{marginTop:20}}><ShieldCheck size={18}/><div><strong>One OAuth account, two developer surfaces</strong><p>MAX Auth remains the authentication source of truth. Use MAX Developers for full permissions, branding, platform identity and manifest configuration.</p></div></section>{error ? <div className="auth-error" role="alert">{error}</div> : null}{loading ? <section className="card"><div style={{height:72,borderRadius:14,background:"var(--surface-soft)"}}/></section> : clients.length ? <section className="card list">{clients.map((client) => <div className="list-row" key={client.id}><div className="icon-box"><KeyRound size={19}/></div><div className="row-main"><div className="row-title">{client.name}</div><div className="row-sub" style={{fontFamily:"monospace"}}>{client.clientId}</div><div className="row-sub">{client.redirectUris.length} redirect URI{client.redirectUris.length === 1 ? "" : "s"} · {client.scopes.length} permission{client.scopes.length === 1 ? "" : "s"}</div></div><span className="status" style={{opacity:client.isActive ? 1 : .55}}>{client.isActive ? "Active" : "Revoked"}</span><Link className="secondary" href={`/applications/${encodeURIComponent(client.id)}/configuration`}><SlidersHorizontal size={14} style={{verticalAlign:"-2px",marginRight:5}}/>Full configuration</Link><Link className="icon-button" href={`/applications/${encodeURIComponent(client.id)}`} title="View application" aria-label={`View ${client.name}`}><Settings2 size={16}/></Link>{client.isActive ? <button className="icon-button" onClick={() => void revoke(client)} disabled={revoking === client.id} title="Revoke application" aria-label={`Revoke ${client.name}`}><Trash2 size={16}/></button> : null}</div>)}</section> : <section className="card" style={{padding:"48px 24px",textAlign:"center"}}><div className="icon-box" style={{margin:"0 auto 16px",width:48,height:48}}><AppWindow size={21}/></div><h2 style={{margin:0,fontSize:17}}>No MAX Auth applications yet</h2><p className="card-desc" style={{maxWidth:460,margin:"8px auto 20px"}}>Create an OAuth client in MAX Auth first. Once registered, it will appear here automatically after you refresh this page.</p><a className="primary" href="https://auth.max-ai.name.ng/developer" target="_blank" rel="noreferrer"><ExternalLink size={15} style={{verticalAlign:"-2px",marginRight:6}}/>Open MAX Auth Developer</a></section>}<div className="footer"><Link href="/">← Back to dashboard</Link></div></main></div></div>;
}
